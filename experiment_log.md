# 실험 로그

시도한 것, 실패한 것, 왜 바꿨는지를 기록한다. 성능 수치만 남기면 결과만 보이고 판단 과정이 사라진다. 포트폴리오에서 설명해야 하는 것은 후자다.

---

## 0. 프로젝트 방향 결정 (2026-09)

### 이전 시도: 공정 단계 인식 (폐기)

YouTube 전극 제조 영상에서 공정 단계를 인식하고 구간별 설명을 생성하는 시스템을 시도했다.

- YOLOv8, 7클래스(Mixer, Current_Collector 4종, Roll_Press_Machine, Slitting_Machine), 201장, mAP50 0.909
- Roboflow 라벨링, RunPod RTX 3090 학습

**문제 1 — 클래스 설계.** `Coated_Electrode`가 코팅·압연·슬리팅 여러 공정에 걸쳐 등장해 공정 라벨링 단계에서 오분류를 유발했다. 전극 클래스를 제거해 회피했으나 근본 해결은 아니었다.

**문제 2 — 문제 유형 미스매치.** "이 장면이 어느 공정인가"는 객체의 위치가 아니라 장면 전체 맥락에 의존한다. 본질적으로 분류 문제인데 검출 도구로 접근했다. 바운딩 박스 라벨링 비용이 프레임당 수십 초로 데이터 확장을 막았고, 소수 클래스(Slitting_Machine) 부족을 생성형 증강(Midjourney)으로 해결하려 했으나 원본 기계 구조가 보존되지 않아 실패했다.

**문제 3 — 실무 가치 부재 (결정적).** 실제 공장에서 공정 단계는 PLC/MES가 이미 알고 있다. 영상으로 추론할 이유가 없다. 데이터를 먼저 확보하고 거기서 할 수 있는 것을 찾은 순서 자체가 잘못이었다.

**교훈**: 문제를 먼저 정의하고 데이터를 구한다. 반대 순서로 하면 기술적으로 완성해도 "왜 필요한가"에 답할 수 없다.

### 현재 방향: 전극 코팅 결함 인라인 검사

**전환 근거**

- 결함 검사는 실제 라인에서 수행하는 작업이며 상당 부분 사람 눈에 의존한다 → 실무 가치가 자명
- 이상 탐지는 정상 데이터만으로 학습 가능 → 결함 라벨링 지옥이 구조적으로 발생하지 않음
- Lombardo et al. (Chem. Rev. 2022)에서 제조 공정 AI 연구는 전체의 6%. 진단·예후 40% 대비 blue ocean. 저자들은 "surprisingly understudied"로 표현
- 동 리뷰 4.3절: 산업 로봇 검사 시스템의 CNN 기반 결함 분류, 데이터 부족 시 One-Class Learning/GAN, 금속 균열·인쇄 검사 전략의 전극 제조 온라인 검사 이전 가능성 명시

### 데이터셋 탐색 기록

| 후보 | 규모 | 판정 | 사유 |
|---|---|---|---|
| **CoatingVision** (Sci Data 2026) | 2,227장 | **채택** | 슬롯다이 코팅, 3종 결함 라벨, figshare 공개, 벤치마크 baseline 제공 |
| 전극 시트 결함 (Sci Rep 2025) | 1,272장 | 보류 | 결함 6종(탭 손상/이물/스크랩/접힘/손상/크랙)으로 실라인에 더 근접하나 공개 여부 불명 |
| BSA Dataset (PRCV 2024) | 5,500장 (정상 5,000) | 보류 | 정상:이상 비율이 이상 탐지에 이상적이나 셀 외관 검사이며 다운로드 경로 불명 |
| Roboflow Universe | - | 기각 | 배터리 물체 검출만 존재, 전극 표면 결함 없음 |
| MVTec AD | - | 불필요 | CoatingVision이 도메인에 직접 부합하므로 우회 불필요 |

**관찰**: 전극 코팅 라인 이미지는 각 사의 핵심 공정 자산이라 공개되지 않는다. CoatingVision 저자들도 자신들이 최초의 전극 코팅 결함 공개 데이터셋이라고 명시했다. 데이터 부재가 이 분야 연구를 막고 있었다는 사실 자체가 프로젝트의 배경이 된다.

### 도메인 갭 판단

CoatingVision은 Vulcan 카본/Nafion/PTFE 기재로 연료전지 계열 조성이다. 배터리 전극이 아니다.

그럼에도 채택한 근거는 **결함 형성 메커니즘이 조성과 무관하게 공유**되기 때문이다. 건조 크랙은 용매 증발 → 모세관압 → 입자 응집 → 필름 수축 → 기공 배출 → 응집력 약화 → 상부에서 기재 방향 전파의 순서로 발생하며, 이는 콜로이드 분산액 건조의 일반 물리다. 박리 역시 접착력이 건조 내부응력을 이기지 못할 때 발생한다는 점에서 동일하다.

이전 불가능한 것은 활물질의 광학 특성(카본블랙과 NMC의 반사율 차이)과 그에 따른 조명·노출 조건, 그리고 절대적 결함 허용 기준이다.

---

## 1. 데이터 구조 확인

(진행 예정)

---

## 2. 정상 패치 추출 검증

**가설**: 이미지의 87.4%가 표면 크랙을 포함하지만, 마스크 기반으로 결함 없는 영역을 패치 단위로 잘라내면 정상 학습 데이터를 확보할 수 있다.

**검증 항목**
- 패치 크기별(32/64/128px) 정상 패치 수확량
- 정상 패치의 시각적 다양성 (코팅 갭·용매비 조건별 분포)
- 결함 경계 인접 패치의 오염 여부

(진행 예정)

---

## 1. 데이터 구조 확인 (완료)

### 디렉토리

```
detection/     images 2227장 + labels 581개 (핀홀만 YOLO 라벨)
classification/ images 2227장 + labels.csv
segmentation/  images 2227장 + masks 2227장
```

세 과제가 **같은 2,227장을 공유**한다. 파일명이 `image_숫자`로 통일되어 있어 과제 간 라벨을 파일명으로 조인할 수 있다. 검출 라벨은 581개(핀홀만)뿐이지만 마스크는 전량 존재하므로, 마스크에서 박리·크랙 정보를 가져와 교차 활용이 가능하다.

### 마스크 포맷 (검증 완료)

- PIL mode `RGBA`, shape `(480, 640, 4)`, dtype `uint8`, 값 `{0, 255}`
- 채널 매핑이 논문과 일치함을 300장 샘플로 확인

| 채널 | 클래스 | 샘플 300장 | 논문 전체 |
|---|---|---|---|
| 0 | Surface Crack | 88.3% | 87.4% |
| 1 | Delamination | 8.0% | 9.1% |
| 2 | Pinhole | 23.0% | 23.3% |
| 3 | Unclassified | 3.7% | - |

샘플 300장 비율이 전체 분포와 일치 → 빠른 실험은 앞쪽 300~600장으로 대표성 확보 가능.

### 예상 밖의 수확: 파일명에 공정 조건이 인코딩되어 있음

`labels.csv`의 `original_file_name` 컬럼 형식:

```
R7-700um-middle_frame_489_patch_1.png
R1-1100um-top-to-bottom-center-1_frame_220_patch_7.png
```

- `R1`, `R7` : 런 번호
- `700um`, `1100um` : **코팅 갭** (논문의 600~1100 µm, 100 µm 간격 조건)
- `middle`, `top-to-bottom-center` : 촬영 위치
- `frame_489` : 원본 영상 프레임 번호
- `patch_1` : 프레임 내 패치 인덱스

**파싱 성공률 100% (2,227건 전부).**

의미가 크다. ① 코팅 갭과 결함률의 관계를 직접 정량화할 수 있어 Advisor 레이어의 근거가 문헌 인용이 아닌 실측이 된다. ② 프레임 번호로 원본 촬영 순서를 복원할 수 있어 SPC 레이어의 시계열이 인위적 나열이 아닌 실제 시퀀스가 된다.

---

## 2. 메타데이터 분석 결과

### 2-1. 코팅 갭 vs 결함률 (논문 서술의 정량 검증)

| gap(µm) | n | crack% | delam% | pinhole% | clean% |
|---|---|---|---|---|---|
| 600 | 306 | 59.2 | 0.0 | 38.2 | 8.2 |
| 700 | 407 | 82.8 | 10.8 | 24.3 | 2.5 |
| 800 | 303 | 81.5 | 16.5 | 28.4 | 2.6 |
| 900 | 304 | 95.7 | 1.6 | 12.8 | 0.3 |
| 1000 | 230 | 98.3 | 9.1 | 20.9 | 0.0 |
| 1100 | 677 | 98.2 | 12.3 | 19.2 | 0.0 |

**크랙**: 59.2% → 98.2% 단조 증가. 갭 증가 = 코팅 두께 증가이고, 두꺼운 코팅일수록 건조 크랙이 증가한다는 논문 서술과 일치. 정상률은 8.2% → 0%로 소멸.

**핀홀**: 600 µm에서 38.2%로 최대, 900 µm에서 12.8%로 최소. **논문에 없는 관찰이다.**

초기 해석("두꺼운 코팅에서 기포가 층 내부에 묻힌다")은 틀렸다. 실제로는 갭-핀홀이 U자형 관계를 가진다.

- **갭 과소**: 고전단 응력으로 압축된 기포가 집전체 통과 직후 팽창·파열 → 작고 많은 미세 핀홀. 응집체가 좁은 갭에 걸려 긁고 지나가면 일렬 배치 핀홀·스크래치 발생
- **갭 과대**: 유동 길이 증가로 공기 포집, 비드 파단과 디웨팅 → 크고 불규칙한 핀홀
- 적정 갭은 통상 목표 wet 두께의 1.5~2.5배

600 µm에서 핀홀이 많은 것은 U자형 곡선의 **좌측 구간**을 관측한 것으로 추정된다. 검증 가능한 예측: 600 µm의 핀홀은 면적이 작고 개수가 많아야 하며, 1100 µm의 핀홀은 크고 roundness가 낮아야 한다. → 02 스크립트에서 검증.

**박리**: 갭과 뚜렷한 상관 없음(0~16.5% 진동). 박리가 코팅 두께보다 기재 접착력과 건조 내부응력에 지배된다는 해석과 일치.

### 2-2. 런 구성의 한계

| run | n | gaps | crack% |
|---|---|---|---|
| R1 | 2,095 (94%) | 600~1100 전 조건 | 88.1 |
| R7 | 132 (6%) | 700만 | 76.5 |

**사실상 단일 런 데이터다.** 논문은 물/IPA 비율을 변화시켰다고 서술했으나 공개된 데이터는 거의 R1 단일 조건이다.

→ **용매비 효과는 이 데이터로 검증할 수 없다.** Advisor가 용매비를 원인 인자로 제시할 때는 문헌 인용으로만 처리하고 실측 근거라고 말하면 안 된다. 이 제약을 문서에 명시한다.

### 2-3. 시계열 가용성

8개 그룹(런×갭×위치), 그룹당 230~404장, 프레임 범위 0~659. 연속 촬영 영상에서 추출된 것이 확인되어 **SPC 레이어에 직접 사용 가능**.

단, 프레임 번호에 결번이 많다(예: R1|600은 0~659 범위에 306장). 저자의 해시맵 중복 제거 때문으로 보인다. **등간격 시계열이 아니므로** 이동평균 계산 시 프레임 번호를 x축으로 두고 불균등 간격을 고려해야 한다.

### 2-4. 치명적 제약: 정상 이미지 44장 (2.0%)

네 클래스가 모두 0인 이미지는 **44장뿐**이다. 예상보다 훨씬 적다.

**이미지 단위 비지도 이상 탐지는 불가능하다.** 44장으로 PatchCore를 학습시키면 정상 분포를 구성할 수 없다.

더 나쁜 것은 편향이다. 44장 중 약 25장이 600 µm 조건에 집중되어 있다(306 × 8.2%). 이대로 학습하면 "600 µm 코팅의 외관"만 정상으로 배우고 다른 갭의 정상 영역을 이상으로 오판한다.

**대응 방향**: 마스크 기반 패치 추출로 전환하되, **코팅 갭 조건별 균등 샘플링**을 필수 요건으로 둔다. → 02 스크립트에서 수확량 검증.

---

## 3. 정상 패치 추출 검증 + 갭별 핀홀 형태 분석

(02_patch_extraction_check.py 실행 예정)

**검증 항목**
- 패치 크기별(32/64/128px) 정상 패치 수확량
- 갭 조건별 균등 확보 가능 여부 (최소 갭 기준 × 6조건)
- 결함 경계 margin 4px 적용 시 손실량
- 갭별 핀홀 면적·개수·roundness → U자형 가설 검증

**로직 검증 완료** (합성 데이터)
- 480×640에 64px 격자 → 70패치. 크랙 1줄(10패치) + 핀홀 1개(1패치) 오염 → 정상 59개. 정확
- roundness: 완전한 원에서 0.887. 래스터화된 윤곽선 둘레가 과대 측정되기 때문이며, 절대값이 아닌 **갭 간 상대 비교**로 사용

---

## 3. 정상 패치 추출 검증 (완료)

### 3-1. 패치 크기 결정: 64px

margin 4px, stride = patch_size(비중첩 격자), 샘플 600장 기준.

| patch | 갭별 균등 확보량 | 판정 |
|---|---|---|
| 32px | 66,048 | 과잉. 크랙 폭이 수 px이라 32px 창에서는 "어두운 선"과 구분할 문맥이 부족 |
| **64px** | **9,102** | **채택** |
| 128px | 498 | 부족. 1000µm 조건에서 83개뿐이라 해당 조건의 정상 외관을 대표 불가 |

64px 갭별 정상 패치 비율: 600µm 64.0% → 1000µm 33.9%. 크랙률 증가와 반대 방향으로 감소하여 데이터 일관성 확인.

**확정 사양**: 64px, margin 4px, 갭별 균등 1,500개씩 = 9,000패치. PatchCore 계열은 수천 개면 충분하고 coreset subsampling으로 1~10%만 메모리 뱅크에 남기므로 여유 있음.

### 3-2. 검증된 구현 로직

- 480×640에 64px 격자 → 70패치. 크랙 1줄(10패치) + 핀홀 1개(1패치) 오염 → 정상 59개. 합성 데이터로 확인
- roundness는 완전한 원에서 0.887. 래스터화 윤곽선 둘레가 과대 측정되므로 **절대값이 아닌 조건 간 상대 비교**로만 사용

---

## 4. 갭별 핀홀 형태 분석 (부분 확인, 일부 관측 범위 밖)

### 4-1. 마스크 병합 가설 → 기각

02에서 per_image가 모든 갭에서 0.9~1.1로 나온 것을 "세그멘테이션 마스크에서 인접 핀홀이 병합되었다"고 추정했으나 **틀렸다.**

검출 라벨(개별 인스턴스 바운딩 박스, 581파일/564박스)로 재측정한 결과 per_image가 0.9~1.06으로 마스크 결과와 동일했다. 병합이 아니라 실제로 패치당 핀홀이 1개꼴이다.

**원인**: 파일명의 `patch_0`, `patch_7`이 단서였다. 이 데이터는 원본 프레임을 이미 잘라낸 **패치**다. 480×640이 전체 코팅면이 아니라 확대 촬영된 소영역이므로 패치당 핀홀 1개가 자연스럽다.

**파생 제약**: 이 데이터로는 **핀홀 밀도(단위 면적당 개수)를 측정할 수 없다.** 패치 단위로 이미 분할되어 있고, 저자의 해시맵 중복 제거로 일부 패치가 누락되어 원본 프레임 재조립이 불가능하다. → Advisor의 판별 규칙에서 "개수" 지표를 제외해야 한다.

### 4-2. 측정 결과

검출 라벨(B) 기준 박스 면적/종횡비, 마스크(A, 전체 2227장) 기준 면적/roundness.

| gap(µm) | B area_median | B area_max | B aspect | A area_median | A roundness |
|---|---|---|---|---|---|
| 600 | 360.0 | 1,040 | 1.21 | 50.5 | 0.718 |
| 700 | 360.0 | **3,685** | 1.21 | 66.0 | 0.739 |
| 800 | 393.5 | **3,127** | 1.19 | 52.0 | 0.687 |
| 900 | 256.0 | 726 | 1.07 | 21.0 | 0.747 |
| 1000 | 308.0 | 1,092 | 1.14 | 38.5 | 0.769 |
| 1100 | 270.0 | 783 | 1.07 | 23.8 | 0.783 |

### 4-3. 판정

**좁은 갭 메커니즘: 지지됨.**

600~800µm 구간에서 세 지표가 같은 방향을 가리킨다.
- 종횡비 1.19~1.21 (vs 900~1100의 1.07~1.14) → 더 길쭉함
- area_max 1,040~3,685 (vs 726~1,092) → 간혹 매우 큰 결함
- roundness 0.687~0.739 (vs 0.769~0.783) → 더 불규칙

"응집체나 이물질이 좁은 갭에 걸려 코팅층을 긁고 지나가며 일렬 배치 핀홀이나 스크래치를 발생시킨다"는 메커니즘과 방향이 일치한다. 종횡비 1.2는 절대값으로는 길쭉하다고 보기 어려우나 조건 간 차이는 일관적이다.

**넓은 갭 메커니즘: 관측되지 않음.**

비드 불안정/공기 포집에 의한 대형 불규칙 핀홀이 넓은 갭에서 증가하는 현상은 나타나지 않았다. 오히려 1100µm 쪽이 더 작고 둥글다.

**해석**: 실험 범위(600~1100µm) 전체가 U자형 곡선의 **좌측 구간**일 가능성이 높다. 적정 갭이 목표 wet 두께의 1.5~2.5배임을 감안하면, 1100µm에서도 비드 불안정 징후가 없다는 것은 이 실험이 과대 갭 영역에 도달하지 않았음을 시사한다. 크랙이 갭에 따라 단조 증가(59.2%→98.2%)한 것과도 정합적이다 — 갭 증가로 코팅은 두꺼워져 크랙은 늘지만 비드는 아직 안정적이라 핀홀은 감소한다.

가설을 기각하는 것이 아니라 **관측 범위의 한계**로 기록한다.

### 4-4. Advisor 판별 규칙 (수정)

per_image 지표는 사용 불가(4-1). 형태 지표로 대체한다.

- 종횡비 중앙값 > 1.2 AND area_max 큼 → 응집체 걸림 / 과소 갭 의심
- roundness 중앙값 < 0.72 → 불규칙 형태, 기계적 원인(긁힘/걸림) 의심
- roundness 중앙값 > 0.78 → 둥근 형태, 기포/젖음성 원인 의심

**임계값 한계**: 6개 갭 조건, 단일 런(R1 94%)에서 도출. 다른 설비·조성으로의 일반화는 검증되지 않았다. Advisor 출력 시 신뢰도를 medium 이하로 표기할 것.

---

## 5. 패치 추출 완료 및 라벨 검증

### 5-1. 산출물

27,711 패치 / 112MB. 64px, stride 32, margin 4px.

| 구분 | 수량 | 용도 |
|---|---|---|
| train/normal | 18,000 (갭당 3,000) | 정상 분포 학습 |
| val/normal | 3,600 | 임계값 결정 |
| test/normal | 3,600 | 오탐률 측정 |
| eval/surface_crack | 1,200 (갭당 200 상한) | 검출률 |
| eval/delamination | 752 | 상한 미달 (갭별 분포 편중) |
| eval/pinhole | 559 | 전량 |

결함 패치는 val+test 분할 이미지에서만 수집했다. 비지도 학습은 정상만 사용하므로 결함 패치 자체는 누수가 아니지만, 출처 이미지가 train과 겹치면 모델이 그 배경에 이미 익숙해진 상태로 평가받게 되므로 train 출처는 배제했다.

### 5-2. 클래스별 밝기/초점 지표

| 클래스 | 밝기 중앙값 | 최대 밝기 | 국소 대비 | 라플라시안 분산 |
|---|---|---|---|---|
| normal | 138.2 | 144.5 | 1.9 | 2.3 |
| surface_crack | 134.3 | 145.5 | 4.8 | 5.0 |
| delamination | 162.5 | **240.5** | 15.1 | 11.2 |
| pinhole | 145.9 | **246.0** | 7.9 | 12.2 |

**클래스별 난이도가 근본적으로 다르다.**

- delamination / pinhole: 최대 밝기가 240 이상으로 정상(144.5)의 1.7배. 하부 PTFE 기재(흰색)가 노출되어 포화에 가까운 밝기가 나온다. **단순 밝기 임계값만으로 상당 부분 검출될 가능성이 높다.**
- surface_crack: 평균 134.3 / 최대 145.5로 정상과 거의 동일. 어두운 선이라 밝기로는 구분 불가. 대비(4.8 vs 1.9)와 라플라시안(5.0 vs 2.3)에서만 차이.

→ **전체 평균 AUROC 하나로 평가하면 안 된다.** 쉬운 클래스가 어려운 클래스를 가린다. 클래스별 개별 평가 필수.
→ **딥러닝 없는 사소한 베이스라인(밝기 통계)을 먼저 측정해야 한다.** PatchCore가 이 바닥을 얼마나 넘는지가 딥러닝 사용의 정당화 근거가 된다.

### 5-3. 초점 아티팩트 가설 → 기각

축소 이미지에서 박리 패치가 흐릿해 보여 "초점이 나간 프레임을 박리로 라벨링한 것 아닌가"를 의심했다. 라플라시안 분산으로 검증한 결과:

| 측정 대상 | n | laplacian_var 중앙값 |
|---|---|---|
| 박리 영역 | 150 | **32.6** |
| 같은 이미지의 정상 영역 | 150 | 4.4 |
| 박리 없는 이미지의 정상 영역 | 150 | 3.1 |

박리 영역이 오히려 **7.4배 선명하다.** 초점 문제가 아니다.

원인: 정상 영역의 라플라시안이 2.3~4.4로 극도로 낮은 것은 균일한 카본 코팅면이라 고주파 성분이 거의 없기 때문이다. 흐려 보인 것은 64px 패치 8장을 붙여 2배 확대한 시각화 방식의 문제였다.

**교훈**: 지표를 세울 때 예상 방향을 미리 정해두면 반대 결과를 해석하지 못한다. 판정 기준(0.5 미만/0.8 이상)이 실제값 7.41을 포괄하지 못했다. 정량 지표는 시각 확인과 병행해야 한다.

### 5-4. delamination 라벨의 실체 (중요)

원본 640×480 + 마스크 오버레이로 6장을 육안 검토한 결과, **박리 라벨의 대부분이 독립적 박리가 아니라 크랙에 부여되어 있었다.**

- 6장 중 5장: 망상 크랙(mud crack) 패턴이며, 빨간 라벨이 **크랙 라인 위, 특히 여러 크랙이 만나는 교차점과 폭이 넓어진 구간**에 걸려 있음
- 1장(image_1501)만 코팅층이 통째로 떠오른 전형적 박리 형태

**추정되는 라벨링 규칙**: 건조 크랙이 진행되어 폭이 벌어지면 그 지점에서 코팅층이 기재로부터 들리기 시작하고, 크랙 가장자리가 말려 올라가며 하부 PTFE가 노출되어 밝게 보인다. 저자들은 이 "크랙이 심화되어 국부 박리로 전이된 지점"을 delamination으로 라벨링한 것으로 보인다.

이것이 5-3에서 박리 영역의 라플라시안이 높았던 진짜 이유다. 흐릿한 박리면이 아니라 **밝은 크랙 에지**였다.

**판단 유보**: 이 해석이 물리적으로 타당한지는 확답할 수 없다. 이 데이터셋은 Vulcan 카본/Nafion/PTFE 조성으로 배터리 전극(NMC/PVdF/Al foil)이 아니며, 배터리 전극 관점에서는 박리보다 크랙에 가까워 보인다. 조성과 기재가 다르므로 실무 경험으로 대조할 수 없다. → 이 자체가 **domain_gap_note에 기록해야 할 구체적 사례**다.

**시사점**: crack과 delamination이 연속적 현상이므로 두 라벨은 물리적으로 중첩된다. 저자 베이스라인에서 delamination Recall이 0.62~0.73으로 가장 낮았던 원인 중 하나일 가능성이 높다. AI 관점의 설명("데이터가 203장뿐이라서")과 별개로, **라벨 정의 자체가 연속적 현상을 이산적으로 나눈 것**이라는 물리적 설명이 가능하다.

**대응(C안 채택)**: 개별 평가와 통합 평가를 모두 수행한다.
- crack, delamination을 각각 평가 → 저자 baseline과 직접 비교 가능
- 두 클래스를 "건조 결함(drying defect)"으로 통합하여 평가 → 통합 시 성능이 오르면 라벨 모호성의 정량적 증거

---

## 6. 이상 탐지 벤치마크 - Part A (밝기 통계 베이스라인)

### 6-1. 결과

test/normal 3,600 vs eval 결함. AUROC / FPR@95TPR.

| 방법 | crack | delamination | pinhole | 통합(drying_defect) | 속도 |
|---|---|---|---|---|---|
| A1 평균 밝기 | 0.451 | 0.794 | 0.594 | 0.583 | 0.09 ms |
| A2 최대 밝기 | 0.519 | 0.974 | 0.991 | 0.695 | 0.10 ms |
| **A3 국소 표준편차** | **0.976** | **0.995** | **0.992** | **0.983** | **0.10 ms** |
| A4 절대편차 | 0.537 | 0.658 | 0.490 | 0.584 | - |

A3 FPR@95TPR: crack 0.117 / delamination 0.001 / pinhole 0.038

### 6-2. 해석

**A3(패치 밝기 표준편차) 하나로 세 클래스 모두 AUROC 0.97~0.99가 나온다.**

원인은 05에서 측정한 국소 대비에 있다. 정상 1.9 / 크랙 4.8 / 핀홀 7.9 / 박리 15.1. 정상 코팅면이 극도로 균일해 표준편차가 2 근처인데, 어떤 결함이든 텍스처를 만들어 값이 2~8배로 뛴다.

**A2가 박리·핀홀만 잡고 크랙을 놓친 것(0.519)은 기재 노출 가설의 정량적 확인이다.** 하부 PTFE(흰색)가 드러나 밝아지는 결함은 최대 밝기로 검출되지만, 어두운 크랙은 배경과 최대값이 같아 검출되지 않는다.

**A4 실패는 설계 오류였다.** 양방향 포착을 의도했으나 0.49~0.66으로 최악이었다. 64×64=4,096px 패치에서 결함이 차지하는 면적이 작아 평균 밝기가 거의 변하지 않는다. 크랙 수십 픽셀이 전체 평균을 움직이지 못한다. **평균 기반 지표는 이 문제에 원리적으로 부적합하다** — 국소 통계(표준편차, 최대값)가 필요하다.

### 6-3. 통합 평가(C안)의 한계

crack + delamination 통합 AUROC가 개별값 사이에 위치할 뿐 개선이 없다(A3: 0.976 / 0.995 → 통합 0.983).

**통합 AUROC는 라벨 모호성을 측정하는 지표가 아니었다.** 난이도가 다른 두 클래스를 섞으면 중간값이 나오는 것이 당연하다. 5-4에서 관찰한 crack/delamination 라벨 중첩을 정량화하려면 다른 방법이 필요하다.

대안: 두 클래스를 구분하는 분류기를 학습시켜 혼동 행렬을 보거나, 두 클래스의 특징 공간 분포 중첩도(예: 실루엣 계수)를 측정하는 방식. → 후속 과제로 이관.

### 6-4. 수치의 낙관 편향 (반드시 명시할 것)

이 AUROC는 실제 인라인 검사 성능보다 높다. 두 가지 이유가 있다.

**① 애매한 패치를 버렸다.** 정상은 margin 4px까지 완전히 깨끗한 것만, 결함은 82px(핀홀 15px) 이상인 것만 선별했다. 그 사이 구간은 학습·평가 어디에도 넣지 않았다. 실제 검사에서는 애매한 영역을 버릴 수 없다.

**② 조명 조건이 단일하다.** 데이터의 94%가 R1 단일 런이다. 표준편차는 "결함이 있는가"가 아니라 "텍스처가 있는가"를 재는 지표이므로, 조명 변동·먼지·초점 흔들림이 있으면 오탐이 급증할 것으로 예상된다.

**③ 갭 조건별 오탐률은 안정적이다.** A3 기준 0.043~0.148로 큰 편차가 없다. 단, 1100µm에서 0.148로 가장 높은데 이는 해당 조건의 크랙률이 98.2%로 최고여서 정상 패치 자체가 크랙 경계에 근접해 있기 때문으로 보인다.

### 6-5. 이 결과의 의미

**"딥러닝이 필요 없었다"는 것이 결론이다. 그리고 이것은 실패가 아니다.**

- 0.1ms짜리 표준편차가 AUROC 0.98을 낸다는 사실을 먼저 확인했다. 이후 어떤 모델을 쓰든 이 바닥을 넘어야 정당화된다
- 인라인 검사는 초당 수백 프레임을 처리해야 하므로 0.1ms와 수 ms의 차이는 라인 속도를 직접 결정한다
- 대부분의 사례가 이 검증을 건너뛰고 딥러닝 성능만 제시한다

**Part B의 목적이 바뀐다.** "성능 향상"이 아니라 **"표준편차가 실패하는 지점에서 작동하는가"**를 본다.
- crack에서 남은 2.4%(FPR 0.117)는 어떤 패치인가
- 조명·텍스처 변동에 대한 강건성
- 저자 지도학습 baseline(YOLOv11 mAP@0.5 = 0.796) 대비 위치

---

## 7. 이상 탐지 벤치마크 - Part B (사전학습 특징)

ResNet18(ImageNet) layer2+layer3 전역평균풀링 → 384차원. train 5,000 / test 3,600. CPU 추론.

### 7-1. 결과

| 방법 | crack | delam | pinhole | 통합 | crack FPR@95 | 통합 FPR@95 | 속도 |
|---|---|---|---|---|---|---|---|
| A3 표준편차 | 0.976 | 0.995 | 0.992 | 0.983 | 0.117 | 0.087 | **0.11 ms** |
| B1 kNN(k=5) | 0.986 | 0.996 | 0.992 | 0.989 | 0.066 | 0.026 | 3.59 ms |
| **B2 Mahalanobis** | 0.984 | **0.998** | 0.990 | **0.990** | **0.052** | 0.029 | **1.27 ms** |
| B3 PatchCore | 0.986 | 0.996 | 0.992 | 0.989 | 0.052 | 0.027 | 2.27 ms |

갭별 오탐률: A3 0.043~0.148 → B2 0.005~0.078 (전 구간 개선, 편차도 축소)

### 7-2. AUROC가 아니라 FPR을 봐야 한다

AUROC만 보면 0.976 → 0.986으로 1%p 차이라 무의미해 보인다. **실무 지표인 FPR@95TPR에서 격차가 드러난다.**

- crack: 11.7% → 5.2% (절반 이하)
- 통합: 8.7% → 2.6% (3분의 1)

하루 10만 패치를 검사한다면 오탐이 8,700건 → 2,600건. **6,100번의 불필요한 정지·재검사가 사라진다.** AUROC 1%p가 실무에서는 6,100건이다.

→ 포트폴리오에서 AUROC를 주 지표로 쓰면 안 된다. FPR@목표TPR을 전면에 둘 것.

### 7-3. PatchCore가 Mahalanobis를 못 이겼다

예상 밖의 결과. 최신 기법인 PatchCore가 더 단순한 마할라노비스와 crack FPR 동률(0.052)이면서 속도는 1.8배 느리다(2.27 vs 1.27 ms).

**원인**: PatchCore의 강점은 정상 분포가 다봉(multi-modal)일 때 발현된다. 여러 종류의 정상 외관이 섞여 있으면 메모리 뱅크가 각각을 기억한다. 그러나 이 데이터의 정상 패치는 **균일한 카본 코팅면 단일 모드**다. 05에서 측정한 정상 패치의 국소 대비 1.9, 라플라시안 2.3이 이를 뒷받침한다. 단봉 분포는 가우시안 하나로 충분히 모델링되므로 메모리 뱅크가 기여할 여지가 없다.

**시사점**: 최신 기법이 항상 우월하지 않다. 데이터의 분포 특성에 맞는 방법을 선택해야 하며, 그 판단 근거는 데이터를 먼저 측정하는 데서 나온다.

### 7-4. 현재까지의 결론과 남은 의문

**딥러닝은 정당화된다. 단, AUROC가 아니라 오탐 감소로.**

트레이드오프가 명확하다.
- 라인이 빠르고 오탐 처리 비용이 낮다 → A3 표준편차 (0.11 ms)
- 오탐 한 건의 비용이 크다(라인 정지, 재검사 인력) → B2 Mahalanobis (1.27 ms, 초당 780패치로 실시간 충분)

**그러나 이 수치들은 여전히 낙관적이다** (6-4 참조). 애매한 패치를 배제했고 조명 조건이 단일(R1 94%)하다.

지금 결과만으로 "1%p 때문에 12배 비용을 쓰라"고 주장하기는 약하다. **조명·노이즈·초점 변동 하에서 A3가 무너지고 B2가 버틴다면** 주장이 완성된다. 표준편차는 "결함이 있는가"가 아니라 "텍스처가 있는가"를 재는 지표이므로 이론적으로는 변동에 취약할 것으로 예상되나, 검증이 필요하다.

→ 다음 실험: 강건성 시험 (08)

---

## 8. 강건성 시험 (08)

### 8-1. 설계

정상 분포 모델은 교란 없는 원본으로 구축하고, 추론만 교란된 입력으로 수행한다. 실제 운영이 그렇다 — 모델은 정상 조건에서 만들어지고 현장 조건이 변한다.

**fixFPR이 핵심 지표다.** 임계값은 교란 없는 조건에서 오탐률 5%가 되도록 정하고, 교란 후에도 그 임계값을 그대로 쓸 때의 오탐률을 잰다. 현장에서 조명이 변했다고 임계값을 즉시 재조정할 수는 없다.

### 8-2. 결과 (fixFPR)

| 교란 | A3 표준편차 | B2 마할라노비스 |
|---|---|---|
| none | 0.050 | 0.050 |
| bright_shift +25 | **0.050** | 0.064 |
| bright_shift -15 | **0.050** | 0.077 |
| bright_gain 1.25 | 0.166 | 0.216 |
| **noise σ=3** | 0.129 | **1.000** |
| **noise σ=8** | 0.996 | **1.000** |
| noise σ=15 | 1.000 | 1.000 |
| blur σ=1.5 | 0.024 | 0.046 |
| block 4px | 0.030 | 0.024 |

AUROC (노이즈 σ=15): A3 **0.979** vs B2 **0.840**

### 8-3. 가설이 정반대로 틀렸다

**예상**: 표준편차는 "텍스처가 있는가"를 재므로 노이즈에 취약하고, ImageNet 사전학습 특징은 다양한 조명/노이즈를 본 표현이므로 강건할 것.

**실제**: 딥러닝이 훨씬 취약했다. σ=3짜리 육안으로 거의 안 보이는 센서 노이즈만으로 B2의 fixFPR이 **100%**가 된다. 모든 정상 패치를 결함으로 판정한다.

**원인**: 마할라노비스 거리는 정상 특징 분포의 **공분산 역행렬**을 사용한다. 정상 패치가 극도로 균일하여(05 측정: 국소 대비 1.9, 라플라시안 2.3) 특징 공간에서 분포가 매우 좁게 뭉쳐 있다. 공분산이 작으면 역행렬이 커지고, **작은 입력 변화가 거리를 폭발적으로 증폭시킨다.** 노이즈는 ImageNet 특징에 고주파 성분을 주입하는데, 정상 분포가 그 방향으로 분산을 갖고 있지 않으므로 즉시 이상치로 판정된다.

표준편차는 노이즈가 들어와도 값이 연속적으로 증가할 뿐이다(σ=3에서 1.17→2.08, σ=8에서 4.78). **예측 가능하게 열화한다.**

**교훈**: 정량화 없이 직관으로 판단하면 안 된다. "단순한 것이 취약하고 복잡한 것이 강건하다"는 통념이 여기서는 반대였다.

### 8-4. 부수 발견

- **밝기 shift에는 A3가 수학적으로 완벽하다** (fixFPR 정확히 0.050 고정). 표준편차는 평행이동에 불변이다. B2는 0.048~0.077로 흔들린다.
- **블러는 오히려 오탐을 줄인다** (5% → 2~5%). 정상 패치가 매끄러워져 이상 점수가 낮아진다. 다만 결함도 함께 흐려지므로 미검출이 늘 것이며, 현재 지표가 이를 포착하지 못한다. → 별도 검증 필요.
- **밝기 gain은 둘 다 취약하다** (1.25배에서 A3 0.166, B2 0.216). 대비가 함께 변하므로 원리적으로 회피 불가.

### 8-5. 실무 결론이 바뀐다

07의 "딥러닝이 오탐을 1/3로 줄인다"는 **조명과 센서가 완벽히 안정적일 때만 성립한다.**

1. **센서 노이즈 억제가 알고리즘 선택보다 우선한다.** 노이즈 제거 전처리를 필수로 넣거나, 카메라 게인을 낮추고 노출을 늘려야 한다.
2. **환경이 불안정하면 표준편차가 낫다.** 성능은 낮지만 예측 가능하게 열화한다.
3. **딥러닝 사용 시 입력 정규화가 필수다.** 블러 결과가 좋았으므로 가우시안 블러 전처리로 노이즈 취약성이 완화될 가능성이 있다. → 후속 검증 과제.

---

## 9. SPC 레이어 (09, 10)

### 9-1. 설계 결정

- **시계열 단위는 프레임.** 한 프레임(480×640)에서 70패치를 뽑아 **이상 패치 비율**로 집계. 평균 점수는 정상 패치가 많으면 희석되지만 비율은 결함 존재를 직접 반영한다.
- **불균등 시계열.** 프레임 번호에 결번이 많으므로(저자의 중복 제거) "최근 N개 샘플" 창을 쓰되 x축은 프레임 번호로 표시. 시간 기반 창은 사용 불가.
- **기준 구간은 각 시퀀스의 앞 30%.** 이 데이터에 "정상 생산 구간"이 따로 없기 때문. 관리도의 전제(기준 구간이 안정 상태)가 완전히 충족되지 않는 한계가 있다.
- **EWMA 초기화 버그 수정.** 첫 관측값으로 초기화하면 초기 관리한계가 매우 좁은 구간에서 그 값 자체가 이탈로 판정되어 거짓 알람이 발생한다. 기준 구간 중심선으로 초기화.

### 9-2. 09 초기 결과: 알람 1,000건

시퀀스당 EWMA만 161건. 실무에서 무시되는 수준(alarm fatigue).

원인은 **한 번 이탈하면 그 상태가 지속되는 동안 매 프레임 알람이 울린 것.** 실무에서는 "이상 구간 진입"을 한 번만 알리면 된다.

### 9-3. 파라미터 튜닝 결과

관리도 유효 시퀀스(3개)만 대상. lift = 알람구간 크랙률 / 비알람구간 크랙률.

| 설정 | 병합후 알람 | 알람구간% | lift |
|---|---|---|---|
| 기본(09) | 318 | 45.5 | 5.94 |
| 병합만 | 38 | 42.1 | 3.38 |
| 3s+EWMA0.1 | 44 | 49.2 | 16.61 |
| **3.5s+EWMA0.1 (권장)** | **39** | **43.3** | **16.51** |
| 4s+EWMA0.1 | 37 | 41.2 | 8.84 |
| 3s Shewhart만 | 27 | 30.3 | 2.66 |
| 4s Shewhart만 | 20 | 22.6 | 1.35 |

**318건 → 39건 (8배 감소) 하면서 lift는 5.94 → 16.51 (2.8배 증가).** 알람을 줄였는데 정확도가 올랐다.

**λ를 낮춘 것이 결정적이었다.** EWMA λ 0.2→0.1만으로 lift가 3.38→16.51. λ가 작을수록 과거를 길게 누적하여 순간 요동에 덜 반응하고 추세만 잡는다. 병합만으로는 부족했고 평활화가 핵심이었다.

**Shewhart 단독은 부적합하다** (lift 2.66, 4σ에서 1.35). 급격한 이탈만 보므로 완만한 결함률 상승을 놓친다. → **이 공정의 이상은 급변이 아니라 드리프트 형태다.** 건조 크랙이 점진적으로 진행되는 물리와 부합한다.

### 9-4. 공정능력 판정 (가장 중요한 결과)

관리도는 **안정된 정상 상태**를 전제한다. 정상 상태 자체가 불량이면 "이상"을 정의할 수 없다.

| 시퀀스 | 중심선 | 판정 |
|---|---|---|
| R1/600µm | 0.042 | **GOOD** |
| R1/800µm | 0.112 | MARGINAL |
| R1/700µm | 0.216 | MARGINAL |
| R1/1100µm | 0.288 | INCAPABLE |
| R1/1000µm | 0.329 | INCAPABLE |
| R7/700µm | 0.345 | INCAPABLE |
| R1/900µm | 0.360 | INCAPABLE |
| R1/1100µm-1 | 0.428 | INCAPABLE |

**8개 중 5개가 INCAPABLE.** 900µm 이상은 전부.

갭별 크랙률(900µm 95.7%, 1000µm 98.3%, 1100µm 98.2%)과 일관된다. 거의 모든 프레임이 결함이면 검사 시스템이 할 일이 없다. **검사가 아니라 공정 조건 변경이 필요하다.**

이것이 Advisor의 첫 번째 출력이 된다:
> 코팅 갭 900µm 이상에서 공정능력이 붕괴되어 있습니다. 관리도 기반 모니터링이 무의미하며, 갭 축소 또는 건조 조건 완화가 선행되어야 합니다. (실측: 600µm 크랙률 59.2% → 900µm 95.7%)

**R7 이상치**: 같은 700µm인데 R1은 MARGINAL(0.216), R7은 INCAPABLE(0.345). 갭이 동일한데 결함이 더 많다는 것은 다른 요인의 작용을 시사하며, 논문이 변화시킨 물/IPA 비율일 가능성이 있다. 단 R7이 132장뿐이라 단정 불가.

### 9-5. 한계

- **INCAPABLE 판정 기준(중심선 0.25)은 임의값이다.** 물리적 근거가 없으며, 실제로는 해당 제품의 품질 규격에서 도출되어야 한다. 조정 가능한 파라미터로 둔다.
- **알람 구간 43%는 여전히 많다.** 실용 수준은 10~15% 이하다. GOOD 시퀀스가 600µm 하나뿐이라 MARGINAL 두 개에 수치가 끌려간 측면이 있다.
- 이상 패치 비율만 보므로 **결함 유형을 구분하지 못한다.** 크랙 증가와 핀홀 증가는 원인도 대응도 다르다. → 객체 검출 레이어가 필요한 이유.

---

## 10. 마스크 -> 검출 라벨 자동 생성 (11, 12, 13)

### 10-1. 접근 근거

검출 라벨은 581파일/564박스(핀홀만)뿐이나 세그멘테이션 마스크는 2,227장 전부 존재한다. 마스크에서 연결 요소를 찾아 바운딩 박스로 변환하면 3클래스 전체로 확장할 수 있다.

손으로 그리지 않은 이유는 노동 절감이 아니다. 저자들이 U-Net 앙상블 pseudo-label + 사람 수정으로 만든 마스크가 새로 그린 것보다 정확하고 일관되기 때문이다. 참고로 48,753 박스를 손으로 그리면 개당 5초로도 68시간이 걸린다.

### 10-2. 첫 검증 실패 (11)

자동 생성 핀홀 박스를 저자 라벨과 IoU 0.3 기준 대조:
- 재현율 0.239, 정밀도 0.289, 매칭된 것의 IoU 중앙값 0.35
- 판정: 불일치

**여기서 곧바로 파라미터를 조정하지 않고 원인 진단으로 넘어간 것이 옳았다.** MIN_AREA를 조정하거나 임계값을 낮췄다면 근본 원인을 놓치고 잘못된 데이터를 만들었을 것이다.

### 10-3. 진단 결과 (12): 검증 기준이 틀렸다

네 가지 가설을 세워 검증했다.

| 가설 | 결과 |
|---|---|
| H3 채널 매핑 오류 | **기각.** class 0 박스 564개 전부(100%)가 채널 2(핀홀) 픽셀 포함. class 1은 85개 전부가 채널 3 대응 |
| H1 좌표계 차이 | **기각.** YOLO 표준(중심) 해석 채움률 0.1395 vs 좌상단 해석 0.0402 |
| H2 박스 크기 관례 | **채택.** 저자 323px vs 마스크 81px, 면적비 3.988 |
| H4 대상 자체가 다름 | 기각 |

시각 확인에서도 초록 박스(저자) 안에 빨간 영역(마스크)이 정확히 중앙에 들어가 있었다. 위치는 완벽하고 크기만 달랐다.

**박스가 4배 크면 중심이 완벽히 일치해도 IoU 상한이 81/323 = 0.25 다.** 임계값 0.3으로 걸렀으니 대부분 탈락한 것이 당연했다.

**변환 로직은 처음부터 옳았고, 서로 다른 라벨링 관례를 IoU로 비교한 검증 설계가 오류였다.**

### 10-4. 수정 및 재검증 (13)

**클래스별 패딩**: 핀홀만 선형 2배(면적 4배) 확대. 근거는 세 가지다.
- 저자 baseline과 비교하려면 같은 라벨링 관례여야 한다. 박스 크기가 다르면 mAP 자체가 비교 불가다
- 작은 객체 검출에서 타이트 박스는 학습이 불안정하다. 핀홀 81px는 640×480에서 극히 작다
- 실무 검사 시스템도 "여기쯤 결함이 있다"를 알려주지 픽셀 경계를 요구하지 않는다

크랙은 선형 결함이라 확대 시 배경만 늘어나고, 박리는 면적 3457px로 이미 충분해 둘 다 1.0 유지.

**검증 기준을 중심 거리로 변경**: 알고 싶은 것은 "저자 박스와 마스크 결함이 같은 것을 가리키는가"이며, 그것은 중심의 근접성으로 판단해야 한다.

| 지표 | 11 (IoU) | 13 (중심거리+패딩) |
|---|---|---|
| 재현율 | 0.239 | **0.830** |
| 정밀도 | 0.289 | **1.000** |
| IoU 중앙값 | 0.35 | **0.711** |
| 중심거리 중앙값 | - | **0.0 px** |

중심 거리 중앙값과 p90이 모두 0.0px다. 픽셀 단위로 같은 위치를 가리킨다. 정밀도 1.0은 생성한 468개 전부가 저자 라벨과 대응한다는 뜻이다.

**남은 17%**: 저자 라벨 564개 중 468개만 생성됨. 96개는 마스크에 존재하지 않는다. 마스크 어노테이션이 검출 라벨보다 보수적이었거나 MIN_AREA 8px에 걸린 것으로 추정. 정밀도가 1.0이므로 생성된 라벨은 신뢰 가능하며, 누락은 한계로 명시한다.

### 10-5. 생성 결과

| class | images | boxes | box/img | area_median |
|---|---|---|---|---|
| surface_crack | 1,947 | 47,627 | 24.46 | 1,230 |
| delamination | 203 | 658 | 3.24 | 3,457 |
| pinhole | 437 | 468 | 1.07 | 330 |

분할: train 1,556(34,380박스) / val 331(6,927) / test 340(7,446)

**클래스 불균형 102:1:1.** 학습 전부터 소수 클래스 실패가 예상되었다.

---

## 11. YOLOv8n 학습 및 평가

### 11-1. 환경 문제 두 건

- **tar 업로드 중단**: 26MB 중 21MB만 전송되어 train 이미지가 1,556 → 184장으로 잘림. labels는 온전한데 images만 빠진 것이 단서였다(tar 순차 기록 특성). md5 대조로 확인 가능
- **data.yaml 경로**: 재압축 시 `path:`가 로컬 절대경로로 되돌아감. 압축 해제 직후 sed 수정이 필요

두 건 모두 학습 자체와 무관하나, 재현 가능한 파이프라인을 만들려면 기록해둘 가치가 있다.

### 11-2. 결과 (RTX 3090, 50 epochs, 4분)

| 클래스 | val mAP@0.5 | **test mAP@0.5** | test P | test R | test mAP@0.5:0.95 |
|---|---|---|---|---|---|
| surface_crack | 0.739 | 0.753 | 0.651 | 0.735 | 0.482 |
| delamination | 0.393 | 0.399 | 0.712 | 0.344 | 0.171 |
| pinhole | 0.907 | **0.858** | 0.627 | **0.863** | 0.526 |
| all | 0.679 | 0.670 | 0.664 | 0.648 | 0.393 |

val과 test가 거의 일치한다. 과적합 없음.

### 11-3. 저자 baseline 비교 (핀홀)

| | mAP@0.5 | mAP@0.5:0.95 | P | R |
|---|---|---|---|---|
| 저자 YOLOv11 (핀홀 단일) | 0.7956 | 0.5208 | 0.7588 | 0.7621 |
| **본 연구 YOLOv8n (test, 3클래스)** | **0.8580** | **0.5260** | 0.6270 | **0.8630** |

mAP@0.5 +6.2%p, Recall +10.1%p. Precision은 -13.2%p로 오탐이 더 많으나, 검사 시스템에서는 미검출보다 오탐이 낫다.

**비교 조건 명시 필수**: 본 연구의 핀홀 라벨은 마스크에서 생성한 것으로 저자 원본의 83%만 재현한다(13 검증). 놓친 96개가 어려운 사례라면 평가가 유리해진 것이므로 완전히 동일한 비교는 아니다.

더 작은 모델(YOLOv8n 3M 파라미터)로 3클래스를 동시 학습하고도 핀홀에서 앞섰다. 데이터가 가장 적은 클래스(468박스)에서 성능이 가장 높은 것은 **결함 형태의 일관성** 때문으로 보인다. 05 측정에서 핀홀은 최대 밝기 246으로 정상(144.5)과 명확히 구분되고 형태도 균일하다. 13의 2배 패딩으로 박스가 18×18 → 36×36px가 된 것도 기여했을 가능성이 있다.

### 11-4. 박리: 05 가설 반증

confusion matrix (normalized):

| True delamination | 예측 |
|---|---|
| delamination | 0.35 |
| **background** | **0.63** |
| surface_crack | **0.00** |

**05에서 세운 "crack/delamination 라벨 중첩" 가설이 이 실험에서는 지지되지 않는다.**

라벨 모호성이 원인이라면 박리가 크랙으로 오분류되어야 하는데 0.00이다. 크랙과 혼동하는 것이 아니라 **아예 검출하지 못한다.** 학습 478박스, test 72 instances로는 부족하다는 순수 데이터 부족 문제로 보인다.

단, 라벨이 크랙 위에 있으므로 모델이 해당 영역을 크랙으로 학습했고 박리 클래스 confidence가 임계값 아래로 떨어졌을 가능성은 남는다. → conf 임계값을 낮춘 재평가로 구분 가능(후속 과제).

### 11-5. 크랙은 검출 문제가 아니다 (중요)

val_batch0_pred.jpg 확인 결과 **크랙 박스가 이미지를 뒤덮어 원본이 보이지 않는다.** 한 장에 수십 개가 겹친다.

실무 관점에서 사용 불가한 출력이다. 검사 시스템이 "여기 크랙 47개 있습니다"라고 하면 작업자가 판단할 수 없다.

원인은 13에서 크랙을 96px 세그먼트로 분할한 설계다. 박스 채움률을 높이려 한 것이 **하나의 크랙을 수십 개 객체로 파편화**시켰다.

**근본적으로 크랙은 개수를 세는 대상이 아니다.** "이 전극에 크랙이 몇 개인가"는 의미가 없고 "크랙이 얼마나 퍼져 있는가"가 실무 질문이다. 따라서 크랙은 세그멘테이션이나 **면적 비율**로 다루어야 하며, 09 SPC가 이미 이상 패치 비율로 그렇게 하고 있다.

**검출기가 실제로 필요한 대상은 핀홀이다.** 개별 객체이고, 위치와 크기가 의미를 가지며, 03에서 도출한 형태 판별 규칙(종횡비, roundness)의 입력이 된다. 성능도 mAP 0.858로 가장 높다.

### 11-6. 속도 및 캐스케이드 구조

| 방법 | 속도 | 역할 |
|---|---|---|
| A3 표준편차 | 0.11 ms/patch | 1차 스크리닝 |
| B2 마할라노비스 | 1.27 ms/patch | 정밀 이상 탐지 |
| YOLOv8n | 55 ms/image (CPU) | 위치·형태 추출 |

빠른 이상 탐지로 걸러내고 이상 판정된 프레임만 검출기로 보내는 캐스케이드가 자연스럽다.

---

## 12. 핀홀 단일 클래스 재학습 (14)

### 12-1. 전환 근거

11 결과에서 클래스별 성격이 근본적으로 달랐다.

| class | test mAP@0.5 | box/img | 판정 |
|---|---|---|---|
| pinhole | 0.858 | 1.07 | 검출에 적합 |
| surface_crack | 0.753 | 24.46 | 검출 부적합 |
| delamination | 0.399 | 3.24 | 데이터 부족 |

크랙은 개수를 세는 대상이 아니다. "크랙이 몇 개인가"가 아니라 "얼마나 퍼져 있는가"가 실무 질문이며, 09 SPC가 이미 이상 패치 비율로 그것을 다룬다. 검출기가 실제로 필요한 대상은 핀홀이다 — 개별 객체이고, 위치/크기가 의미를 가지며, 03의 형태 판별 규칙(종횡비, roundness)의 입력이 된다.

### 12-2. 데이터 구성

세 가지 후보를 비교했다.

| 구성 | 이미지 | 핀홀박스 | 양성비율 |
|---|---|---|---|
| positive_only | 437 | 468 | 100% |
| **balanced (채택)** | **874** | **468** | **50%** |
| all | 2,227 | 468 | 19.6% |

**balanced 채택 근거**: positive_only로 학습하면 모델이 "모든 이미지에 핀홀이 있다"고 학습하여 깨끗한 전극에도 박스를 그린다. 11에서 핀홀 Precision이 0.627로 낮았던 원인 중 하나로 의심되었다. 배경 이미지를 넣으면 "여기엔 없다"를 학습한다. all은 배경이 80%가 되어 양성 샘플이 희석되고 학습 시간이 5배 늘어난다.

train 610(핀홀305+배경305) / val 124 / test 140

### 12-3. 결과 (4회 반복, seed 0~3)

RTX 3090, 100 epochs, patience=25. 조기 종료로 평균 70에폭.

| 지표 | 평균 | 표준편차 | 범위 |
|---|---|---|---|
| Precision | 0.829 | ±0.025 | 0.806~0.854 |
| Recall | 0.909 | ±0.022 | 0.878~0.928 |
| **mAP@0.5** | **0.924** | **±0.012** | **0.910~0.936** |
| mAP@0.5:0.95 | 0.597 | ±0.032 | 0.576~0.643 |

**mAP@0.5 표준편차 ±0.012로 매우 안정적이다.** 학습 곡선에서 에폭별 mAP50이 0.56~0.94를 오갔으나, 이는 에폭 단위 변동일 뿐 최종 선택 모델은 시드와 무관하게 일관되다. 첫 실행값(0.917)이 평균(0.924)보다 낮았으므로 운 좋은 값이 아니었다.

### 12-4. 최종 비교

| | mAP@0.5 | mAP@0.5:0.95 | P | R |
|---|---|---|---|---|
| 저자 YOLOv11 (핀홀 단일) | 0.7956 | 0.5208 | 0.7588 | 0.7621 |
| 본 연구 3클래스 (test) | 0.8580 | 0.5260 | 0.6270 | 0.8630 |
| **본 연구 단일 (4회 평균)** | **0.924±0.012** | **0.597±0.032** | **0.829±0.025** | **0.909±0.022** |
| 개선폭 | +12.8%p | +7.6%p | +7.0%p | +14.7%p |

**저자 값(0.7956)이 본 연구의 최소값(0.910)보다 낮다.** 시드 변동을 감안해도 우위가 뒤집히지 않는다. 더 작은 모델(YOLOv8n 3M 파라미터)로 달성했다.

**3클래스 → 단일에서 Precision이 0.627 → 0.829로 +20.2%p 개선된 것이 핵심이다.** 배경 이미지 50% 구성과 102:1:1 불균형 제거의 효과다. 3클래스에서는 크랙 47,627개에 묻혀 핀홀 판별 경계가 흐려져 있었다.

### 12-5. 반드시 명시할 조건

1. **라벨 출처가 다르다.** 본 연구의 핀홀 라벨은 세그멘테이션 마스크에서 생성한 것으로 저자 원본 564개 중 468개(83%)만 재현한다. 놓친 96개가 어려운 사례라면 평가가 유리해진 것이므로 완전히 동일한 비교는 아니다.
2. **test instances 73개로 표본이 작다.** 몇 개 차이로 지표가 흔들릴 수 있다. 4회 반복으로 분산을 측정한 것이 이 한계에 대한 대응이다.
3. 저자는 YOLOv11, 본 연구는 YOLOv8n으로 아키텍처가 다르다.

### 12-6. 최종 역할 분담

이 실험으로 각 결함의 물리적 성격에 맞는 방법이 정리되었다.

| 결함 | 방법 | 근거 |
|---|---|---|
| pinhole | **객체 검출** (YOLOv8n) | 개별 객체, 위치/형태가 의미를 가짐. mAP 0.924 |
| surface_crack | **면적 비율** (SPC 이상 패치 비율) | 개수가 아닌 확산 정도가 실무 질문. 검출 시 박스 파편화 |
| delamination | **한계 명시** | 478박스로 학습 불가. Recall 0.34 |

"모든 결함을 하나의 검출기로"가 아니라 결함별로 다른 접근을 택한 것이 이 프로젝트의 방법론적 결론이다.
