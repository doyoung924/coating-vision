# Flask 대시보드 및 파이프라인 실행에 필요한 의존성.
# 앱이 실제 import 하는 것만 명시. 나머지는 transitive 의존성으로 딸려 온다.
# 버전은 현재 .venv 설치본에 고정.

# --- 웹앱 (§2단계) ---
Flask==3.1.3
numpy==2.5.3
torch==2.14.0
ultralytics==8.4.144
segmentation_models_pytorch==0.5.0
opencv-python==5.0.0.93
pillow==12.3.0

# --- DB (§3단계 3-1) ---
SQLAlchemy==2.0.52
oracledb==26.0.0
python-dotenv==1.2.3
